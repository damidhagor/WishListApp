@inherits ComponentBase
@rendermode @(new InteractiveServerRenderMode(false))

<div class="modal modal-lg fade" id="@($"item{WishlistItem?.Id}")" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">Bearbeiten</h2>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            @if (WishlistItem is not null)
            {
                <div class="modal-body">
                    <div class="d-flex flex-column gap-y-2">
                        <h5>
                            <a href="@WishlistItem.Url" target="_blank">
                                Produktseite öffnen
                                <i class="bi bi-box-arrow-up-right ms-2"/>
                            </a>
                        </h5>

                        <h5 class="mt-4">Name</h5>
                        <input type="text" @bind="_name">

                        <h5 class="mt-4">Beschreibung</h5>
                        <input type="text" @bind="_description">

                        <h5 class="mt-4">Bemerkung</h5>
                        <input type="text" @bind="_note">

                        <h5 class="mt-4">Preis</h5>
                        <input type="text" @bind="_price">

                        <h5 class="mt-4">Priorität</h5>
                        <WishlistItemPriorityDropdownComponent WishlistItem="WishlistItem" PriorityChanged="OnPriorityChanged" />
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="LoadCrawledProductInformation">
                        <i class="bi bi-arrow-clockwise" /> Produktinformationen abrufen
                    </button>

                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal" @onclick="SaveWishlistItem">
                        <i class="bi bi-floppy" /> Speichern
                    </button>
                </div>
            }
        </div>
    </div>
</div>